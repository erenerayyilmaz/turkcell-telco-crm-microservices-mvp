spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/catalog_db
    username: ${DB_USER:product_catalog_user}
    password: ${DB_PASS:secret}
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
