spring:
  datasource:
    url: jdbc:postgresql://localhost:5439/billing_db
    username: ${DB_USER:billing_user}
    password: ${DB_PASS:secret}
    driver-class-name: org.postgresql.Driver
  # --- order-events tuketicisi (OrderConfirmed -> bill_cycle; inbox idempotent) ---
  cloud:
    function:
      definition: consumeOrderEvents
    stream:
      bindings:
        consumeOrderEvents-in-0:
          destination: order-events
          group: billing-service-group
      kafka:
        binder:
          brokers: ${KAFKA_BROKERS:localhost:9092}
