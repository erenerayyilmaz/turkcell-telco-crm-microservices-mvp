# Prod profilinde API dokumantasyonu varsayilan olarak disariya acilmaz.
# Internal developer portal veya private network ihtiyacinda env ile acilabilir.
springdoc:
  api-docs:
    enabled: ${SPRINGDOC_API_DOCS_ENABLED:false}
  swagger-ui:
    enabled: ${SPRINGDOC_SWAGGER_UI_ENABLED:false}
